/**
 * 书写完全硬更新，包括检测是否有更新，检测之后提示更新，强制更新，手动更新
 * */

import React from 'react';

const updateurl = '';

class updateapp {
    static GetVersion(version, callback){
        callback(version);
    }

    static show_update(version){

    }

    static must_update(version){

    }

}



export default updateapp;

